<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">


    <!--指定IOC容器扫描的包
        通过resource-pattern指定扫描的资源

    <context:component-scan
            base-package="annotation.zmxka.annotation"
            resource-pattern="repository/*.class"/>
    -->

    <!--指定IOC容器扫描的包-->

    <!--exclude-filter 不包含什么-->
    <!--include-filter 制定包含哪些表达式的组件,子节点需要use-default-filters = false 配合使用-->
    <!--annotation 通过注解筛选
        assignable 通过类名筛选
    -->
    <!--<context:component-scan base-package="annotation.zmxka.annotation"-->
    <!--use-default-filters="false">-->
        <!--<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>-->
        <!--<context:include-filter type="annotation" expression="org.springframework.stereotype.Repository"/>-->
        <!--<context:exclude-filter type="assignable" expression="annotation.zmxka.annotation.repository.UserRepository"/>-->
        <!--<context:include-filter type="assignable" expression="annotation.zmxka.annotation.repository.UserRepository"/>-->
    <!--</context:component-scan>-->

    <context:component-scan base-package="annotation.zmxka.annotation"/>

</beans>